<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Ohne diese Abfrage sieht die App ab Android 11 den Spracherkenner
         des Systems nicht und meldet fälschlich „nicht verfügbar“. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <!-- `allowBackup="false"` sperrt die Sicherung auf allen Fassungen,
         `dataExtractionRules` zusätzlich den Umzug auf ein neues Telefon
         ab Android 12. Der verschlüsselte Schlüssel wäre anderswo ohnehin
         nicht zu lesen, aber er hat dort auch nichts verloren. -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/datenabgabe"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/Theme.Jarvis">

        <!-- Hochkant festgehalten: ein Chat am Handy, mit der Eingabe unten. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Jarvis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
